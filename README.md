# 🍅 POMODORO TIMER

Un timer pomodoro simple pero eficiente para la terminal, construido en Python con Rich

## ✨ Características

- ⏱️ Timer visual con una barra de progreso
- 🎨 Interfaz colorida y moderna para la terminal
- ⚡ Actualización en tiempo real

## Instalación

### Prerrequisitos
- Python 3.7+
- Terminal compatible con colores

### Pasos

1. Clona el repositorio:
  ```bash
  git clone https://github.com/GoguX76/pomodoro-timer
  cd pomodoro-timer
  ```
  
2. Crea el entorno virtual
  ```bash
  python -m venv venv
  source venv/bin/activate #Si usas Linux/Mac
  venv\Scripts\activate #Si usas Windows
  ```
    
3. Instala las dependencias
  ```bash
  pip install -r requirements.txt
  ```

### Uso
Ejecuta el siguiente comando en la terminal:
  ```bash
  python main.py
  ```

## ⚙️ Configuración

- **WORK_TIME**: Dentro del módulo timer.py, esta variable indica la duración del trabajo. Por defecto, es 1500, pero puedes modificarla para tener un valor mayor o menor.
- **BREAK_TIME**: Dentro del módulo timer.py, esta variable indica la duración del descanso. Por defecto, es 300, pero puedes modificarla para tener un valor mayor o menor.

## 🤝 Contribuciones
¡Las contribuciones son bienvenidas! Siéntete libre de abrir issues o pull requests.

## 📄 Licencia
Este proyecto está bajo la Licencia MIT.

## Requisitos para su correcto funcionamiento
- La terminal debe ejecutarse a pantalla completa para que los ASCII se muestren correctamente.

## IMPORTANTE
El proyecto se encuentra en una fase temprana, apenas tiene lo básico para funcionar y hay cosas que no se utilizan aún. Esta sección desaparecerá cuando el proyecto tenga un mayor progreso.
